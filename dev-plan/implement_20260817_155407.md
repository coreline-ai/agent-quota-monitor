# implement_20260817_155407.md

작성 일시: `2026-08-17 15:54:07 KST`

이 문서는 Grok quota 시간 창을 공식 근거에 맞게 재검증하고, QuotaBeacon에 Gemini CLI Provider를 안전하게 추가하는 개발 범위를 고정하기 위한 작업 문서다.

## 개발 목적

- Grok에 5시간 quota가 있다는 추정과 현재 공식 계약을 분리한다.
- 공식 xAI 응답에 없는 Grok 5시간 수치를 만들거나 표시하지 않는다.
- 로그인된 공식 Gemini CLI가 제공하는 quota만 모델 호출 없이 읽어 QuotaBeacon의 다섯 번째 Provider로 표시한다.
- Gemini Code Assist 내부 서비스나 OAuth credential을 QuotaBeacon이 직접 호출·복제하지 않는다.
- 안정적인 기계 판독 계약이 확인되지 않으면 숫자를 추정하지 않고 명시적인 미지원 상태를 표시한다.

## 개발 범위

- xAI 공식 문서·설치된 Grok CLI·redacted billing 응답을 이용한 Grok window 계약 재검증
- Grok `periodType` schema drift 탐지와 주간/월간/알 수 없는 창의 보수적 정규화
- 공식 Gemini CLI 실행 파일·버전·인증 방식의 비밀정보 없는 탐지
- 기존 Gemini CLI의 `/stats model`과 ACP surface 조사 후, 공식 Antigravity CLI `/usage` transport 확정
- 공식 Gemini CLI 프로세스 경계 안에서 실행되는 read-only quota 수집 adapter
- Gemini 모델 quota의 `remainingFraction`, reset 시각, 모델/등급 레이블 정규화
- `ProviderID.gemini`, 설정, 메뉴바, 대시보드, 필터, 추세, 데이터 소스 화면 통합
- synthetic/redacted fixture, parser/provider/UI 회귀 테스트
- Provider 계약·제품 범위·보안·README 문서 업데이트

## 제외 범위

- 공식 응답으로 관측되지 않은 Grok 5시간 quota의 하드코딩 또는 추정
- Grok API의 RPS/TPM을 Grok 구독 quota로 합산
- Gemini 웹 앱, Gemini 모바일 앱 quota와 Antigravity의 Claude·GPT 모델 그룹 통합
- QuotaBeacon이 `oauth_creds.json`, `google_accounts.json`, API key 또는 refresh token을 읽는 구현
- QuotaBeacon이 Gemini Code Assist의 `retrieveUserQuota` 등 내부 endpoint를 직접 호출하는 구현
- 설치된 Gemini CLI bundle 내부 JavaScript를 runtime API나 링크 라이브러리로 사용하는 구현
- quota 확인을 위한 모델 prompt, 채팅, tool call, agent hook 실행
- Gemini API key·Vertex AI·조직 billing quota의 1차 구현
- Gemini 사용량이 없을 때 요금제의 정적 일일 한도만으로 사용률을 계산하는 구현
- 다른 Provider 추가, 대시보드 전면 재설계, 신규 외부 dependency 도입

## 참조 문서

- [정본 개발 계획](implement_20260816_133341.md)
- [제품 범위](../docs/product-scope.md)
- [아키텍처](../docs/architecture.md)
- [Provider 계약](../docs/provider-contracts.md)
- [보안·개인정보](../docs/security-privacy.md)
- [Provider 실환경 검증](../docs/provider-validation-2026-08-16.md)
- [xAI Grok Usage & Limits FAQ](https://docs.x.ai/grok/faq)
- [xAI API Rate Limits](https://docs.x.ai/developers/rate-limits)
- [Gemini CLI quota and pricing](https://github.com/google-gemini/gemini-cli/blob/main/docs/resources/quota-and-pricing.md)
- [Gemini CLI command reference](https://github.com/google-gemini/gemini-cli/blob/main/docs/reference/commands.md)
- [Gemini CLI terms and privacy](https://github.com/google-gemini/gemini-cli/blob/main/docs/resources/tos-privacy.md)

## 사전 검토 결과와 결정 기록

### Grok

- [x] 2026-08-17 기준 xAI 공식 FAQ는 유료 Grok 사용량을 Chat, Imagine, Voice, Build 등에 공유되는 **주간 사용량 풀**로 설명한다.
- [x] 현재 공식 FAQ와 로컬 Grok CLI `1.0.4` artifact에서 구독용 5시간 window 근거를 찾지 못했다.
- [x] xAI API의 RPS/TPM은 API team·model별 rate limit이므로 Grok 구독 quota와 별도 계약으로 취급한다.
- [x] 현재 제품 기본 표시는 `주간 공용`을 유지한다.
- [x] 향후 official billing 응답에 실제 5시간 `periodType`이 redacted probe로 관측되기 전에는 `.fiveHour`로 매핑하지 않는다.

### Gemini

- [x] 개발 장비에서 공식 Gemini CLI `0.55.1`과 Antigravity CLI `1.1.13` 설치를 credential 내용 열람 없이 확인했다.
- [x] 공식 문서는 `/stats model`이 현재 session token 통계와 quota 정보를 제공한다고 설명한다.
- [x] 설치된 공식 CLI는 모델별 quota bucket에 잔여 비율과 reset 시각을 표시하지만 top-level 비대화형 quota command는 제공하지 않는다.
- [x] Google의 현재 문서는 Google 계정 요금제에 일일 최대 request 수를 명시하지만, 이 정적 상한만으로 현재 사용률을 계산하지 않는다.
- [x] Google 문서는 Gemini CLI를 구동하는 서비스에 third-party 도구가 직접 접근하는 것을 금지하므로 내부 Code Assist endpoint 직접 호출은 채택하지 않는다.
- [x] 실제 검증에서 Gemini CLI 개인 Google login은 Antigravity migration 오류를 반환했고 ACP에는 stats command가 없어 `/stats model`을 안전하게 사용할 수 없음을 확인했다.
- [x] 공식 Antigravity CLI `/usage`가 Gemini 모델 그룹의 주간·5시간 잔여율을 모델 요청 없이 표시함을 확인했다.
- [x] 권장 수집 경로를 `공식 Antigravity CLI /usage PTY`로 변경하고 Claude·GPT group은 제외한다.

## 과거 실패·교훈 참조

- 검색 상태: `실행 완료`
- 검색 일시: `2026-08-17 KST`
- 검색 대상 scope:
  - `AIQuotaMonitor/Providers/Grok`
  - `AIQuotaMonitor/Providers/Gemini`
  - `AIQuotaMonitor/Domain`
  - `AIQuotaMonitor/Features/Settings`
  - `AIQuotaMonitorTests`
  - `Scripts/ProviderProbe`
  - `docs`
- 검색 결과: `1건`
- `DL-20260816T100934Z-63002bfd` — **adopted**
  - 이유: Gemini도 top-level help에는 quota command가 없지만 `/stats model`, ACP, 설치 artifact의 nested contract가 존재할 가능성이 있다.
  - 예방 control: 최상위 help만 보고 미지원 판정을 내리지 않고 nested command, ACP/schema, 공식 source, redacted read-only 실제 응답을 순서대로 확인한다.
  - 적용 task: Phase 1의 capability matrix와 transport gate.
  - 적용 test: `Scripts/ProviderProbe/test_gemini_cli_quota_probe.py`의 top-level command 부재·nested surface 존재 회귀 테스트.

## 문서 기반 제약 사항

- 기존 `docs/product-scope.md`의 “네 Provider 외 확장”은 이 작업에서 **Gemini CLI 한정**으로 변경한다. 다른 Provider 제외 정책은 유지한다.
- 값이 없으면 퍼센트를 표시하지 않고 `nil`, 0%, 100%를 구분한다.
- Provider 실패는 다른 Provider refresh와 history 저장을 막지 않는다.
- cache를 LIVE로 표시하지 않으며 source, contract, observed time, freshness를 유지한다.
- 모델 호출·credential write-back·browser cookie 수집은 계속 금지한다.
- Gemini 외부 CLI는 QuotaBeacon에 복사·링크·번들하지 않는다.

## 예상 변경 파일 / 영향 범위

| 영역 | 예상 파일 | 책임 |
|---|---|---|
| Domain | `AIQuotaMonitor/Domain/QuotaModels.swift` | `ProviderID.gemini`, Gemini window 식별 |
| 표시 | `AIQuotaMonitor/DesignSystem/QuotaPresentation.swift` | Gemini symbol, short label, tint, preference key |
| Grok | `AIQuotaMonitor/Providers/Grok/GrokQuotaParser.swift` | 관측된 period만 정규화, unknown drift 보존 |
| Gemini | `AIQuotaMonitor/Providers/Gemini/**` | CLI locator, process adapter, parser, redaction |
| 앱 배선 | `AIQuotaMonitor/Application/QuotaMonitorModel.swift` | Gemini provider 생성·설정 적용 |
| migration | `AIQuotaMonitor/Application/LegacyPreferencesMigrator.swift` | 신규 visibility/connection key 보존 |
| 설정 | `AIQuotaMonitor/Features/Settings/**` | 자동 찾기, 연결 토글, 근거·실패 안내 |
| 화면 | `AIQuotaMonitor/Features/MenuBar/**`, `Overview/**`, `Limits/**`, `Trends/**`, `DataSources/**` | 다섯 Provider 표시와 필터 회귀 |
| Probe | `Scripts/ProviderProbe/gemini_cli_quota_probe.py` 외 | 무모델·redacted capability 검증 |
| Tests | `AIQuotaMonitorTests/Providers/Gemini/**`, `AIQuotaMonitorTests/Fixtures/Gemini/**` 외 | parser, process, UI/domain 회귀 |
| Docs | `README.md`, `docs/product-scope.md`, `docs/provider-contracts.md`, `docs/architecture.md`, `docs/security-privacy.md`, `docs/fixture-manifest.md` | 범위·계약·제약·검증 결과 |

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 material 이슈는 해당 Phase에서 수정하고 occurrence 후보를 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 Provider·인증 방식·endpoint로 범위를 확장하지 않는다.
- 숫자 연결은 공식 문서 하나만이 아니라 공식 CLI 표면과 redacted 실제 응답까지 확인한 뒤 활성화한다.
- Gemini CLI 실행은 timeout, cancellation, stdout 상한, executable validation을 적용한다.
- 실제 모델 request가 발생하거나 agent hook이 실행된 정황이 있으면 해당 transport를 즉시 폐기한다.
- raw ANSI/TUI output, 이메일, account ID, project ID, token, home path는 저장·로그하지 않는다.
- quota bucket에 없는 plan 상한·reset 주기·공유 관계를 추정하지 않는다.
- unknown model과 unknown period는 parser 실패가 아니라 명시적 custom/partial 상태로 보존한다.
- 새 dependency 없이 Foundation `Process`와 기존 process abstraction을 우선 사용한다.
- 외부 프로젝트 code, fixture, UI, asset은 복사하지 않는다.

## Phase 상태 요약

- [x] Phase 0. 계약 기준과 제품 범위 갱신 완료
- [x] Phase 1. Gemini 안전 capability probe와 transport 확정 완료
- [x] Phase 2. Grok 계약 hardening 완료
- [x] Phase 3. Gemini Provider adapter 완료
- [x] Phase 4. 앱·GUI·history 통합 완료
- [x] Phase 5. 자동·실환경·보안 QA 완료
- [x] Phase 6. 문서·패키지·릴리스 준비 완료

## QA 관점

- [x] Grok weekly/monthly/unknown period와 reset 누락을 각각 검증한다.
- [x] `FIVE_HOUR` 문자열을 synthetic fixture에 넣는 것만으로 Grok 5시간 기능이 활성화되지 않는지 검증한다.
- [x] Gemini remaining fraction의 nil/0/1/음수/1 초과/NaN/infinity 경계를 parser와 공통 `QuotaRatio` 테스트로 검증한다.
- [x] Gemini bucket 일부 누락, 고정 bucket label, reset 누락을 검증한다. `/usage`는 모델별 bucket을 제공하지 않아 unknown model 병합은 하지 않는다.
- [x] Gemini CLI 없음·권한·output drift·hang/crash·ANSI 손상을 locator/runner/parser typed state로 검증하고 locale은 executor에서 `en_US.UTF-8`로 고정한다.
- [x] 미로그인은 authentication state로 구분하고 API key/Vertex/조직·직접 HTTP status 처리는 CLI 경계 밖 MVP 제외로 확인했다.
- [x] quota probe가 `/usage` 외 prompt/model/tool/agent command를 실행하지 않는지 검증한다.
- [x] 직접 Code Assist endpoint 호출이나 credential 파일 열람이 제품 source에 없는지 검사한다.
- [x] Provider 수가 4에서 5로 늘어도 exhaustive switch, visibility, trends, notification, migration이 깨지지 않는지 검증한다.
- [x] stale cache가 live로 보이지 않고 Gemini 실패가 다른 네 Provider refresh를 막지 않는지 검증한다.
- [x] 메뉴바 compact/detail, 작은 창 스크롤, AX identifier 기반 접근성 smoke를 XCUITest로 검증했다. 실제 VoiceOver 낭독·Increase Contrast 수동 QA는 후속 비차단 항목으로 남긴다.
- [x] 설치 앱 Gemini snapshot을 실제 비율 없이 official `/usage` 결과와 대조했다.

## 교훈 분류 요약

- [x] 계획 작성 시점 신규 발생 후보 없음
- [x] `OCC-GEMINI-001`은 `existing-reference` — 기존 `DL-20260816T100934Z-63002bfd`의 nested capability control로 안전한 transport 전환을 완료했다.
- [x] `OCC-GEMINI-002`는 `new-lesson` — `DL-20260817T075548Z-4aa0bdee`로 child slave PTY size control을 보존했다.
- [x] `OCC-GEMINI-003`은 `new-lesson` — `DL-20260817T081820Z-b30f558d`로 반복 terminal query가 단계 timeout을 재시작하지 않게 하는 control을 보존했다.
- [x] `OCC-PROCESS-001`은 `plan-only` — 구현 중 신규 cancellation 경계 테스트가 즉시 발견·수정했으며 별도 재발/QA escape 없이 tracked regression test로 충분하다.

---

## Phase 0. 계약 기준과 제품 범위 갱신

### 목표

현재 공식 근거에 맞춰 Grok 5시간 주장과 Gemini 지원 범위를 문서로 고정한다.

### 구현 태스크

- [x] `docs/provider-contracts.md`에 Grok 주간 공용 계약과 “5시간 미관측”을 기록한다.
- [x] xAI API RPS/TPM이 구독 quota와 별도임을 명시한다.
- [x] Gemini Google account의 Antigravity migration과 API key/Vertex 정적 quota가 제품 실시간 bucket과 다름을 구분한다.
- [x] Gemini MVP를 공식 Antigravity CLI login의 read-only `/usage`로 제한한다.
- [x] `docs/product-scope.md`의 Provider 수와 표시 목록을 5개로 변경한다.
- [x] `docs/architecture.md`와 `docs/security-privacy.md`에 CLI process boundary와 금지 endpoint를 기록한다.
- [x] contract evidence 표에 공식 URL, 확인 일자, 안정성, 미지원 시 상태를 기록한다.

### 자체 테스트

- [x] Grok 5시간이 현재 기능처럼 표현된 문장이 문서에 없다.
- [x] Gemini 정적 일일 상한과 현재 사용률이 같은 값으로 서술되지 않는다.
- [x] Gemini 내부 endpoint 직접 호출 금지가 scope·architecture·security 세 문서에 일치한다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 문서 계약 승인
- [x] 자체 테스트 완료
- [x] Phase 1 진행 가능

## Phase 1. Gemini 안전 capability probe와 transport 확정

### 목표

모델 호출 없이 공식 Gemini CLI가 quota를 내보내는 안전하고 반복 가능한 경계를 확인한다.

### 구현 태스크

- [x] CLI locator가 공식 user-local/Homebrew 설치 경로와 사용자 지정 절대 경로를 순서대로 탐색하게 한다.
- [x] executable owner, 실행 권한, symlink 거부, version을 redacted evidence로 수집한다.
- [x] top-level help, `/stats model`, `/model`, `--acp`, Antigravity `/usage` surface를 capability matrix로 조사했다.
- [x] ACP command registry에 stats가 없고 `/stats model`을 prompt로 보내면 모델 요청으로 이어질 수 있음을 확인해 폐기했다.
- [x] 기존 Gemini CLI 개인 login이 Antigravity migration 대상으로 거부됨을 실제 CLI에서 확인했다.
- [x] 공식 Antigravity CLI의 bounded PTY에서 `/usage`가 Gemini 주간·5시간 quota를 모델 요청 없이 표시함을 확인했다.
- [x] bounded PTY adapter는 `/usage`만 전송하고 Gemini group marker 뒤 결과만 수집하게 구현한다.
- [x] 실행 전후 model request/tool count와 prompt 미전송을 독립적으로 검사했다.
- [x] quota 결과가 없을 때 plan 상한으로 보완하지 않고 `unsupportedContract`를 반환한다.
- [x] probe output은 capability boolean, CLI version, bucket field 존재 여부만 남긴다.
- [x] transport 결정과 폐기 근거를 `docs/provider-contracts.md`에 기록한다.

### 자체 테스트

- [x] top-level quota command가 없어도 nested `/stats model`, ACP, Antigravity `/usage`를 별도로 탐지했다.
- [x] command가 모델 prompt로 해석될 가능성이 있는 기존 Gemini CLI transport를 폐기했다.
- [x] timeout/cancel/비정상 종료 후 child process가 남지 않도록 terminate·wait를 검증한다.
- [x] fixture와 diagnostic에 email, token, account/project ID, 절대 home path가 없다.
- [x] 지원하지 않는 output/version drift는 숫자 대신 typed `unsupportedContract`를 반환한다.

### 이슈 및 수정

- [x] `OCC-GEMINI-001 | 2026-08-17 16:04 KST | 기존 Gemini CLI 개인 OAuth가 Antigravity migration 오류로 거부되고 ACP에 stats command가 없음 | 최초 Gemini CLI /stats transport 계획을 사용할 수 없음 | gemini 0.55.1 실제 로그인 오류·ACP command registry·Google migration 문서 확인 | 공식 Antigravity CLI 1.1.13 /usage의 Gemini group 전용 PTY transport로 계획 변경`
- [x] `OCC-GEMINI-002 | 2026-08-17 16:24 KST | bounded Expect 실행이 TUI 본문 없이 terminal control query만 반환 | Gemini group을 찾지 못해 실제 연결 검증이 중단됨 | child PTY output 약 190 bytes·group marker 없음, 동일 CLI의 interactive /usage는 정상 | spawn child slave PTY에 48x120 크기를 지정한 뒤 redacted probe 재실행`

### 완료 조건

- [x] 무모델 transport 확정
- [x] redacted 실제 응답 확보
- [x] 자체 테스트 완료
- [x] Phase 2 진행 가능

## Phase 2. Grok 계약 hardening

### 목표

현재 주간 계약은 정확히 유지하고 미래 schema 변경이 잘못된 5시간 표시로 이어지지 않게 한다.

### 구현 태스크

- [x] `GrokQuotaParser`의 weekly/monthly/unknown period 매핑을 명시적인 switch로 정리한다.
- [x] unknown `periodType` 원문은 저장하지 않고 `공용 크레딧` fallback과 redacted probe category만 남긴다.
- [x] 실제 official response에서 5시간 period가 관측되기 전에는 `.fiveHour` 매핑을 추가하지 않는다.
- [x] reset이 없는 응답은 `.partial`, 값이 없는 응답은 가짜 0%가 아닌 empty window로 유지한다.
- [x] safe probe에 period category와 reset field 존재 여부를 추가한다.
- [x] 기존 주간 history/window identity가 migration 없이 유지되는지 확인한다.

### 자체 테스트

- [x] weekly fixture가 `주간 공용`으로 표시된다.
- [x] legacy monthly/unknown fixture가 custom window로 표시된다.
- [x] synthetic unknown/five-hour-like 문자열이 5시간으로 오인되지 않는다.
- [x] malformed percent와 reset 누락이 typed error/partial로 처리된다.
- [x] 기존 Grok billing provider 회귀 테스트가 통과한다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] Grok parser·probe 완료
- [x] 자체 테스트 완료
- [x] Phase 3 진행 가능

## Phase 3. Gemini Provider adapter

### 목표

Phase 1에서 승인된 공식 CLI transport를 최소 책임 단위로 구현한다.

### 구현 태스크

- [x] `GeminiCLIRuntimeLocator`를 구현한다.
- [x] `GeminiCLIQuotaExecutor`에 timeout, cancellation, combined output byte limit, environment allowlist를 적용한다.
- [x] `GeminiQuotaParser`가 주간·5시간 bucket label, remaining fraction, reset duration만 정규화한다.
- [x] `usedRatio = 1 - remainingFraction`을 검증된 범위 안에서만 계산한다.
- [x] 두 공유 bucket은 독립 window로 유지하고 임의 병합하지 않는다.
- [x] group의 model description은 수치 계약으로 사용하지 않아 모델명 변경이 window identity를 깨지 않게 한다.
- [x] provenance를 `.officialCLI · .observed · .live`로 설정한다.
- [x] 5분 성공 cache와 기존 `SnapshotStore` last-known-good stale fallback을 연결한다.
- [x] 실제 quota 수치가 없으면 session token 통계를 quota 퍼센트로 대체하지 않는다.

### 자체 테스트

- [x] 정상 다중 bucket fixture가 독립 quota window로 변환된다.
- [x] remaining 100%/0%가 used 0%/100%로 정확히 변환된다.
- [x] invalid fraction, missing reset, missing label과 고정 bucket uniqueness를 검증한다.
- [x] runner timeout/cancel/output overflow와 executor/provider 실패 상태를 검증한다.
- [x] raw CLI output·계정 line·다른 모델 group이 history나 diagnostic에 저장되지 않는다.

### 이슈 및 수정

- [x] `OCC-PROCESS-001 | 2026-08-17 17:01 KST | ProcessRunner task 취소가 polling sleep 중 발생하면 CancellationError가 그대로 반환 | typed cancellation 계약 불일치와 child cleanup 누락 가능성 | 신규 cancellation 경계 테스트에서 기대 .cancelled 대신 다른 error 관측 | sleep 취소 catch에서 terminate·wait 후 .cancelled로 정규화`

### 완료 조건

- [x] Provider adapter 완료
- [x] unit test 완료
- [x] Phase 4 진행 가능

## Phase 4. 앱·GUI·history 통합

### 목표

Gemini를 기존 네 Provider와 동일한 수준으로 연결·표시하되 UI 밀도와 migration을 보존한다.

### 구현 태스크

- [x] `ProviderID.gemini`와 display/symbol/tint/short label을 추가한다.
- [x] 모든 exhaustive switch를 컴파일러 기준으로 갱신한다.
- [x] 설정에 Gemini CLI 자동 찾기, 경로, read-only 토글, evidence, 적용 상태를 추가한다.
- [x] `gemini.readOnlyEnabled`, `gemini.executablePath`, visibility preference를 migration 목록에 추가한다.
- [x] 메뉴바 compact/detail, overview, limits, trends, data sources에 Gemini를 추가한다.
- [x] Provider가 5개일 때 기본 창 높이, scroll 범위, sticky 영역 침범을 점검한다.
- [x] Gemini 두 window가 추세에서 잘못 연결되지 않게 기존 provider/window/reset stable series key를 사용한다.
- [x] Gemini 알림은 reset이 있고 fresh한 실제 quota window에만 허용한다.
- [x] 연결 해제 시 process와 cache 정책을 기존 Provider와 일치시킨다.

### 자체 테스트

- [x] `ProviderID.allCases` 기반 presentation/migration 테스트가 5개를 검증한다.
- [x] 설정의 자동 찾기·적용·refresh·연결 해제 배선과 설치본 적용을 검증한다.
- [x] 메뉴바와 대시보드에서 숫자 중복·잘림·잘못된 창 label이 없다.
- [x] 5개 Provider에서 이전 scroll overlay 회귀가 없다.
- [x] Gemini 실패 상태에서도 Claude/Codex/Grok/GLM이 정상 refresh된다.

### 이슈 및 수정

- [x] popover XCUITest의 status item attach race를 test-only idempotent retry로 안정화했다.

### 완료 조건

- [x] GUI 통합 완료
- [x] migration·UI test 완료
- [x] Phase 5 진행 가능

## Phase 5. 자동·실환경·보안 QA

### 목표

가짜 quota, credential 노출, 모델 호출, Provider 간 회귀가 없음을 자동 테스트와 실제 CLI 대조로 확인한다.

### 구현 태스크

- [x] `xcodebuild` build/test와 Provider probe Python test를 실행한다.
- [x] secret/PII scan, originality audit, dependency/license audit를 실행한다.
- [x] 사용자의 명시적 승인 아래 Gemini `/usage`와 QuotaBeacon 표시를 redacted 방식으로 대조한다.
- [x] probe가 prompt를 보내지 않고 model/tool count를 증가시키지 않으며 설정 fingerprint가 불변임을 확인한다.
- [x] `agy 1.1.13` version evidence와 label/output drift의 typed failure 상태를 확인한다.
- [x] app relaunch·5분 cache·stale merge·rapid coalescing을 자동/설치 smoke로 검증했다. 실제 sleep/wake·network offline은 후속 비차단 수동 QA로 남긴다.
- [x] menu bar/dashboard AX smoke test를 수행했다. 실제 VoiceOver 낭독·키보드 전체 순회는 후속 비차단 수동 QA로 남긴다.
- [x] Grok 주간 표시와 기존 Claude 5시간 표시가 서로 회귀하지 않는지 확인한다.

### 자체 테스트

- [x] Debug/Release build 성공
- [x] unit/UI/probe test 성공
- [x] secret/PII/originality audit 성공
- [x] 모델 request 0건과 credential write 0건 확인
- [x] 설치 앱에서 Gemini `available`, 주간·5시간 두 live window, diagnostic 없음 확인

### 이슈 및 수정

- [x] 수동 dashboard 프로세스와 XCUITest runner가 충돌한 1회 실패는 프로세스 종료 후 깨끗한 환경에서 재실행해 PASS했다.
- [x] `OCC-GEMINI-003 | 2026-08-17 17:14 KST | 같은 로그인·workspace의 /usage가 대체로 수 초 내 성공하지만 설치 Release의 한 시도가 외부 38초 제한까지 종료되지 않음 | Gemini가 연결되어 있어도 failed/timeout으로 노출됨 | history의 시작·종료 시각과 redacted timeout code, 즉시 재시도 성공을 대조 | terminal query branch에 exp_continue -continue_timer, 45초 외부 guard, TERM 뒤 KILL fallback을 적용하고 probe 3회·설치본으로 재검증`

### 완료 조건

- [x] 자동 QA 완료
- [x] 실환경 대조 완료
- [x] Phase 6 진행 가능

## Phase 6. 문서·패키지·릴리스 준비

### 목표

사용자가 Grok과 Gemini 계약 차이, 연결 방식, 한계를 README와 앱에서 오해하지 않게 한다.

### 구현 태스크

- [x] README Provider 표와 기능·설정·troubleshooting을 Gemini 포함 5개로 갱신한다.
- [x] Grok은 현재 주간 공용이며 5시간 window가 미확인임을 기록한다.
- [x] Gemini 정적 plan quota와 실시간 `/usage` bucket을 분리해 설명한다.
- [x] 내부 endpoint·credential 직접 접근을 하지 않는다는 보안 경계를 기록한다.
- [x] `THIRD_PARTY_NOTICES.md`에 외부 설치형 Antigravity CLI가 번들되지 않고 Google 약관을 별도로 따름을 반영한다.
- [x] fixture manifest와 provider validation 보고서를 갱신한다.
- [x] Universal Release build와 ad-hoc 설치 smoke test를 수행한다.
- [x] 계획 체크박스, 발생 이슈, Dev Lesson disposition을 최종 상태로 갱신한다.

### 자체 테스트

- [x] README·제품 범위·Provider 계약의 Provider 목록이 일치한다.
- [x] 문서 링크와 공식 근거 URL이 유효하다.
- [x] package에 Gemini CLI, credential, raw quota payload가 포함되지 않는다.
- [x] 설치본에서 기존 네 Provider와 Gemini 설정 화면이 정상 열린다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 문서·패키지 완료
- [x] 전체 blocking QA 통과
- [x] 계획 종료 가능

## 최종 결과 요약

- Grok Build는 공식 근거와 실제 billing contract에서 구독용 5시간 window가 확인되지 않아 `주간 공용`만 유지했다.
- Gemini는 공식 Antigravity CLI `agy 1.1.13`의 `/usage`에서 Gemini 그룹만 격리해 `주간 공용`·`5시간` 두 window를 표시한다.
- 단위 테스트 `66/66`, macOS UI 테스트 `2/2`, Provider probe 테스트 `33/33`, Gemini redacted live probe `3/3`이 통과했다.
- security, fixture, originality, greenfield, Dev Lesson validation이 모두 통과했다.
- Universal `x86_64 arm64` Release를 `/Users/hwanchoi/Applications/QuotaBeacon.app`에 설치하고 실행했다.
- 설치본 history에서 Gemini `available`, `sharedWeekly`·`fiveHour`, `officialCLI`·`observed`·`live`, diagnostic 없음을 실제 비율·계정정보 없이 확인했다.
