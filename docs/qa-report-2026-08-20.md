# QuotaBeacon 최종 QA 보고서

실행일: `2026-08-20 KST`

이 문서는 2026-08-20 기준 코드 릴리스 후보의 완료 근거를 기록한다. 과거 QA 보고서는 해당 시점의 검증 이력으로 보존하며, 이 문서가 디스크 사용량·Provider 순서 회귀 보강 이후의 최신 결과다.

## 완료 범위

- 메뉴바 popover의 `/` 내부 디스크 사용률과 metadata로 판정한 local·non-internal `/Volumes` 후보의 선택적 외장 badge
- 디스크 정보를 quota snapshot, history, export, notification과 분리하는 Domain·Infrastructure·Application·View 경계
- Provider 표시 순서의 visibility 변경, connected/attention style filter, 설정 재진입, 앱 재실행 후 popover persistence 회귀
- Debug 전체 테스트, Universal ad-hoc Release build, 로컬 ZIP/checksum 및 정적 감사

## 제외 범위

- Developer ID certificate 서명, notarization, stapling, `spctl` 공개 배포 판정
- 외부 업로드·공지·다운로드 링크·clean-account 설치
- Sparkle feed·자동 업데이트·장기 idle/sleep-wake soak·수동 접근성 QA

위 항목은 인증서, Apple 계정, 배포 채널 또는 별도 수동 환경이 필요한 외부 운영 gate이며 이 코드 개발 완료에서 PASS로 표시하지 않는다.

## 자동 검증 결과

| 항목 | 결과 | 근거 |
|---|---|---|
| Debug XCTest/XCUITest | PASS | `95 passed / 0 failed / 0 skipped` |
| Provider order unit regression | PASS | `ProviderDisplayOrderTests` 7건 |
| Provider order UI persistence | PASS | 설정 재정렬, Gemini visibility 왕복, 설정 재진입, 재실행 popover 순서 1건 |
| Disk usage unit/UI regression | PASS | `DiskUsageTests` 8건 및 menu badge UI assertion |
| Universal Release | PASS | `x86_64 arm64`, hardened runtime, ad-hoc signing |
| Signature integrity | PASS | `codesign --verify --deep --strict` |
| ZIP/checksum | PASS | `QuotaBeacon-0.1.0-macOS.zip` SHA-256 manifest 확인 |
| Fixture guard | PASS | synthetic fixture 21개 |
| Provider probe self-tests | PASS | safe validation 17, Claude 2, Grok 4, GLM 4, Gemini 6 |
| Originality/Greenfield/Security | PASS | reference identifier, credential/PII literal 검사 통과 |
| Diff hygiene | PASS | `git diff --check` |

## 핵심 경계 확인

- 디스크 service는 `DiskUsageProviding` 계약을 통해 메뉴바에 주입되며, provider refresh·credential·network·history·export·notification 흐름을 변경하지 않는다.
- 외장 후보가 없거나 조회에 실패하면 외장 badge만 숨기고 기존 Provider 목록과 dashboard CTA는 유지한다.
- Provider 순서는 display layer에서만 적용된다. visibility와 scope filter는 정렬된 배열을 filter하므로 남은 Provider의 상대 순서를 바꾸지 않는다.
- UI test 전용 기본값 초기화는 `AIQUOTAMONITOR_UI_TEST_RESET_DEFAULTS=1`일 때만 동작하며 일반 실행의 설정 저장 형식은 변경하지 않는다.

## 로컬 산출물

- Release app: `/tmp/QuotaBeaconReleaseDerivedData/Build/Products/Release/QuotaBeacon.app`
- Package: `dist/QuotaBeacon-0.1.0-macOS.zip`
- Test result: `/tmp/QuotaBeaconFinalQA/Logs/Test/Test-AIQuotaMonitor-2026.08.20_15-20-45-+0900.xcresult`

`dist/`와 `/tmp` 산출물은 repository source의 일부가 아니며 공개 배포 산출물로 간주하지 않는다.

## 최종 상태

코드와 로컬 검증 기준의 릴리스 후보는 완료됐다. 외부 배포 운영 gate는 명시적으로 제외되어 있으며, 필요한 인증서·Apple 계정·배포 권한이 제공될 때 별도 작업으로 진행한다.
