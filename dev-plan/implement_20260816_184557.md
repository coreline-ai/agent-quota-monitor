# implement_20260816_184557.md

작성 일시: `2026-08-16 18:45:57 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

Grok Build `1.0.4`와 공식 xAI 공개 source에서 확인된 CLI billing backend를 QuotaBeacon에 연결한다. 기존 최상위 help 검사에서 발생한 false negative를 수정하고, 사용자가 명시적으로 승인한 경우에만 `grok login` credential을 read-only로 사용하여 주간 사용률·reset·선불 크레딧을 표시한다.

## 개발 범위

- 공식 Grok Build source가 사용하는 `GET /v1/billing?format=credits` 전용 read-only Provider adapter
- `~/.grok/auth.json`의 owner·regular file·`0600` 검증과 메모리 내 credential 선택
- `creditUsagePercent`, `currentPeriod`, legacy billing field를 처리하는 신규 독립 parser
- Grok read-only 연결 opt-in 설정과 auth file 경로
- `grok agent stdio` capability 검사와 redacted 실계정 probe
- 정상·부분·오류·malformed fixture와 adapter contract test
- README, Provider 계약, 보안, QA, 검증 보고서의 status-only 판정 수정

## 제외 범위

- 외부 참조 source·fixture·구조의 복사 또는 제품 bundle 포함
- 브라우저 cookie, grok.com WebView, gRPC-web fallback 사용
- login, logout, reauth, OAuth refresh, credential write-back
- credential 값·계정 ID·이메일의 log/history/export/fixture 저장
- 모델 호출, prompt 전송, coding turn 생성
- `x.ai/auto-topup-rule`, 모델 API 또는 billing 외 endpoint 호출
- Z.ai GLM·Claude bridge·배포 서명 변경

## 참조 문서

- 이전 검증 계획: [implement_20260816_181617.md](implement_20260816_181617.md)
- 정본 구현 계획: [implement_20260816_133341.md](implement_20260816_133341.md)
- [Provider 계약](../docs/provider-contracts.md)
- [보안·개인정보](../docs/security-privacy.md)
- [xAI 공식 Grok Build billing extension](https://github.com/xai-org/grok-build/blob/main/crates/codegen/xai-grok-shell/src/extensions/billing.rs)
- [xAI 공식 Grok Build ACP dispatch](https://github.com/xai-org/grok-build/blob/main/crates/codegen/xai-grok-shell/src/agent/mvp_agent/acp_agent.rs)
- [Grok Usage & Limits](https://docs.x.ai/grok/faq)
- [CodexBar Grok provider 문서](https://github.com/steipete/CodexBar/blob/main/docs/grok.md) — 동작 비교 전용, source 재사용 금지

## 과거 실패·교훈 참조

- 검색 상태: `실행 완료`
- 검색 일시: `2026-08-16 18:44 KST`
- 검색 대상 scope: `AIQuotaMonitor/Providers/Grok`, `AIQuotaMonitorTests/Providers`, `Scripts/ProviderProbe`, `docs`
- 검색 결과: `0건`
- disposition: 적용할 과거 Lesson 없음

## 확인 필요 사항 / 결정 기록

- 설치된 `grok 1.0.4 (d846eb93d94d)`의 `grok agent --help`와 binary string에서 `agent stdio`, `x.ai/billing`, billing response field를 확인했다.
- 공식 xAI source에서 `x.ai/billing`이 `GET {cli-chat-proxy}/billing?format=credits`와 local login credential을 사용함을 확인했다.
- 설치된 `1.0.4`의 `grok agent stdio`에 실계정 `x.ai/billing`을 호출하면 `-32601 Method not found`가 반환되어 현재 제품 경로로 사용할 수 없다.
- 공식 source와 참조 구현에 동일하게 나타나는 first-party CLI billing backend를 `observed · Beta` 계약으로 사용한다.
- 실계정 direct read-only probe는 HTTP 200과 `config.creditUsagePercent/currentPeriod/prepaidBalance/isUnifiedBillingUser` field를 확인했고 credential hash·mtime·mode는 불변이었다.
- QuotaBeacon은 auth file을 검증 후 access token과 user ID만 메모리에서 선택하며 refresh token은 선택·사용·전송하지 않는다.
- 외부 참조 구현은 계약·오류·timeout 비교에만 사용하고 신규 Swift 구현과 fixture는 직접 작성한다.
- Grok 계약 등급은 `observed`, 출시 표기는 `Beta`로 한다. CLI version 변경 시 fixture와 실계정 smoke test를 다시 실행한다.

## 예상 변경 파일 / 영향 범위

- `AIQuotaMonitor/Providers/Grok/GrokBillingProvider.swift`
- `AIQuotaMonitor/Providers/Grok/GrokCredentialReader.swift`
- `AIQuotaMonitor/Providers/Grok/GrokQuotaParser.swift`
- `AIQuotaMonitor/Application/QuotaMonitorModel.swift`
- `AIQuotaMonitor/Features/Settings/DashboardSettingsView.swift`
- `AIQuotaMonitor/Features/DataSources/DataSourcesView.swift`
- `AIQuotaMonitorTests/Providers/ProviderParserTests.swift`
- `AIQuotaMonitorTests/Providers/GrokBillingProviderTests.swift`
- `AIQuotaMonitorTests/Fixtures/Grok/**`
- `Scripts/ProviderProbe/safe_validation.py`
- `Scripts/ProviderProbe/test_safe_validation.py`
- `Scripts/ProviderProbe/grok_billing_probe.py`
- `README.md`, `CHANGELOG.md`, `docs/**`

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 실제 Grok payload, 사용률, 이메일, 계정 ID, token은 repository와 stdout에 저장하지 않는다.
- network request는 allowlist HTTPS billing URL의 GET으로 제한하고 redirect·cookie·response cache를 사용하지 않는다.
- access token은 Authorization header 구성에만 사용하며 진단·history·export에 전달하지 않는다.
- 누락된 사용률을 0으로 만들지 않고 부분 상태 또는 명시적 실패로 처리한다.
- 기존 프로젝트 API와 Foundation만 사용하며 새 dependency를 추가하지 않는다.

## Phase 상태 요약

- [x] Phase 1. 계약·false negative 수정 기준 확정
- [x] Phase 2. Grok parser·adapter 구현
- [x] Phase 3. 설정·표시·검증 도구 연결
- [x] Phase 4. 회귀·보안·실계정 검증
- [x] Phase 5. 문서·계획 종료

## QA 관점

- [x] ratio 공통 모델의 0·100·NaN/무한대와 Grok parser의 범위 밖·누락을 검증한다.
- [x] weekly/unknown `currentPeriod.type`, legacy monthly fallback과 reset 누락을 검증한다.
- [x] legacy `monthlyLimit/used` fallback은 양수 denominator에서만 사용한다.
- [x] 고정 billing URL·GET·header·malformed response를 안전하게 처리한다.
- [x] credential validator·만료·401·403·429·5xx·timeout을 typed state로 분류한다.
- [x] 사용자 opt-in OFF에서는 Grok provider가 `notConfigured`이고 credential 접근·network request가 발생하지 않는 조립 경계를 확인한다.
- [x] token, email, user ID, 실제 quota 수치가 report·fixture·export에 저장되지 않는다.
- [x] Claude·Codex·Z.ai 및 history/notification 회귀를 전체 29개 unit test로 확인한다.

## 교훈 분류 요약

- [x] `OCC-GROK-001`을 `new-lesson`으로 분류했다. 잘못된 계약 가정이 material rework를 만들었고 재사용 가능한 capability 조사 control이 확인되었다.
- 기록: [`DL-20260816T100934Z-63002bfd`](../docs/dev-lessons/DL-20260816T100934Z-63002bfd.md)

## Phase 1. 계약·false negative 수정 기준 확정

### 목표

공식 CLI 계약, 허용 method, field mapping과 기존 검사 오류를 고정한다.

### 구현 태스크

- [x] 공식 xAI source의 billing handler와 ACP dispatch를 확인한다.
- [x] 설치된 Grok CLI의 `agent stdio`와 embedded billing contract를 확인한다.
- [x] 기존 `grok --help` 단일 검사가 nested RPC를 놓친 원인을 확인한다.
- [x] scope 기반 Dev Lesson 검색을 실행한다.
- [x] 신규 request/response mapping과 typed error 정책을 확정한다.

### 자체 테스트

- [x] `x.ai/billing`이 공식 CLI source와 설치 binary 양쪽에 존재한다.
- [x] 금지 endpoint·credential 저장·browser fallback이 제외 범위에 명시되어 있다.

### 이슈 및 수정

- [x] `OCC-GROK-001 | 2026-08-16 18:38 KST | top-level help 검사만으로 Grok quota 미지원 판정 | Grok LIVE 연결 누락 및 문서 오판 | 공식 source와 설치 binary에서 x.ai/billing 확인 | root cause: 최상위 command를 전체 machine contract로 가정 | 예방: nested help·schema/protocol·binary marker·공식 source·redacted probe를 독립 확인 | 검증: safe validation 15 PASS 및 실계정 RPC/backend 결과 분리 | new-lesson 기록`

### 완료 조건

- [x] 구현 기준 확정
- [x] 자체 테스트 완료
- [x] Phase 2 진행 가능

## Phase 2. Grok parser·adapter 구현

### 목표

공식 first-party CLI billing backend와 검증된 local login credential에만 의존하는 독립 read-only Grok Provider를 구현한다.

### 구현 태스크

- [x] 공식 현재/legacy billing shape parser를 구현한다.
- [x] auth file validator·credential reader와 billing GET request를 구현한다.
- [x] error code·timeout·malformed payload를 typed snapshot으로 변환한다.
- [x] recording HTTP client 기반 adapter contract test를 구현한다.
- [x] 신규 synthetic fixture와 manifest hash를 갱신한다.

### 자체 테스트

- [x] parser 정상·부분·legacy·malformed test가 통과한다.
- [x] adapter URL은 allowlist billing endpoint로 고정되며 GET 외 요청을 보내지 않는다.
- [x] refresh token을 선택·사용·전송하지 않고 access token을 저장·출력하지 않는다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 3 진행 가능

## Phase 3. 설정·표시·검증 도구 연결

### 목표

사용자 opt-in 설정과 Provider 상태 화면, capability probe를 실제 adapter에 연결한다.

### 구현 태스크

- [x] Grok 활성화 toggle과 auth file path를 추가한다.
- [x] `QuotaMonitorModel` Provider 조립을 상태 전용에서 billing adapter로 변경한다.
- [x] 데이터 소스 계약·인증 설명을 `observed · 공식 CLI`로 변경한다.
- [x] safe validation이 nested `grok agent --help`/billing evidence를 검사하게 수정한다.
- [x] redacted Grok billing backend probe를 추가한다.

### 자체 테스트

- [x] opt-in OFF/ON Provider 조립이 구분된다.
- [x] capability 검사와 검증 보고서가 nested billing evidence와 현재 RPC `method_not_found`를 구분한다.
- [x] UI에 cookie·직접 token 사용으로 오해할 문구가 없다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 4 진행 가능

## Phase 4. 회귀·보안·실계정 검증

### 목표

fixture·unit·build·security 검사를 통과하고 실제 Grok 계정에서 read-only 결과를 redacted 형태로 검증한다.

### 구현 태스크

- [x] Swift unit test와 Debug build를 실행한다.
- [x] Python probe test, fixture guard, originality/security audit를 실행한다.
- [x] 실행 전후 Grok credential hash·mtime·mode를 비교한다.
- [x] 실계정 CLI RPC probe에서 현재 `-32601`을 redacted 형태로 확인한다.
- [x] 실계정 billing backend probe를 실행하고 field 존재 여부만 기록한다.
- [x] 실제 사용률·token·계정 정보가 저장되지 않았음을 확인한다.

### 자체 테스트

- [x] 모델 호출·prompt 전송·credential mutation이 없다.
- [x] 기존 Provider 회귀 test가 통과한다.
- [x] 실계정 성공 또는 typed failure가 redacted 보고서에 남는다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 검증 완료
- [x] 자체 테스트 완료
- [x] Phase 5 진행 가능

## Phase 5. 문서·계획 종료

### 목표

Grok 계약 등급·현재 상태·잔여 위험을 정본 문서와 계획에 일치시킨다.

### 구현 태스크

- [x] README·CHANGELOG·아키텍처·Provider 계약을 갱신한다.
- [x] 보안·QA·검증 보고서·참조 등록부를 갱신한다.
- [x] occurrence 후보를 분류하고 필요한 Dev Lesson을 기록한다.
- [x] 계획 체크박스와 최종 결과를 실제 검증 상태로 갱신한다.

### 자체 테스트

- [x] `Grok status-only/unsupportedContract` 잔재가 과거 오판을 설명하는 Lesson 외에는 없다.
- [x] 문서의 계약 등급과 앱 표시가 일치한다.
- [x] 변경 파일에 secret·PII가 없다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 최종 결과 문서화 완료

## 최종 결과 요약

- Grok `observed · Beta` LIVE adapter, parser, opt-in UI, redacted validation probe를 구현했다.
- 실제 계정에서 first-party CLI billing backend HTTP 200과 필요한 field 존재를 확인했으며 credential SHA-256·mtime·mode는 불변이었다.
- Swift unit `29 PASS`, XCUITest `1 PASS`, Python probe `19 PASS`, fixture `16 PASS`, 보안·독창성·Greenfield audit가 모두 통과했다.
- 설치된 Grok Build `1.0.4`의 ACP `x.ai/billing`은 계속 `-32601`이며 제품은 ACP 대신 공식 client의 billing backend만 사용한다.

## 잔여 리스크 / 후속 과제

- Grok billing 계약은 공개 stable API가 아닌 공식 client에서 관측한 Beta 계약이다. Grok Build version·endpoint·response field가 변경되면 연결이 typed failure로 전환될 수 있다.
- Grok Build 변경 시 capability test, synthetic fixture, Swift test, redacted 실계정 probe를 다시 실행한다.
- Z.ai GLM은 별도 공식 machine contract 또는 공식 bridge가 확인될 때까지 상태 전용으로 유지한다.
