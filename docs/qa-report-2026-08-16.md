# QuotaBeacon QA Report

실행일: `2026-08-16 KST`

## 자동 검증

| 항목 | 결과 | 근거 |
|---|---|---|
| Debug build | PASS | macOS arm64, source warning 0 |
| Unit test | PASS | 48 tests: Domain, Claude OAuth/Keychain, Codex/Grok adapters, parser, local usage, persistence, HTTP, process, credential, notification, refresh, GUI·trend presentation 규칙 |
| Dashboard/Popover XCUITest | PASS | 2 tests: dashboard 전체 navigation·한도 scroll clipping·Reset Bands 추세 그래프·설정 control, 실제 NSStatusItem popover 요약→상세 렌더 변화 |
| Fixture guard | PASS | 18 synthetic fixtures, sensitive value/key 0 |
| Originality audit | PASS | 제품·test source에서 참조 프로젝트 고유 식별자 0 |
| Security audit | PASS | shipping source·fixture credential/PII literal 0 |
| Universal Release | PASS | `arm64`, `x86_64`, hardened runtime, ad-hoc local signing |
| Code signature integrity | PASS | ad-hoc app `codesign --verify --deep --strict` |
| ZIP/checksum | PASS | `dist/QuotaBeacon-0.1.0-macOS.zip` 및 SHA-256 |
| Provider validation tool | PASS | Python 자체 테스트 22건: safe validation 16, Claude OAuth probe 2, Grok billing probe 4; 별도 legacy bridge test 1 |
| Codex real-account probe | PASS | 공식 `account/rateLimits/read`, normalized field만 관측, credential 불변 |
| Claude real-account OAuth usage | PASS | Keychain 자동 탐색, Anthropic usage GET HTTP 200, 5시간·7일·Fable field 존재, credential 불변, 설치 앱 3-window LIVE |
| Grok real-account billing probe | PASS | first-party CLI billing backend HTTP 200, 계약 field 존재만 기록, credential SHA-256·mtime·mode 불변 |

최종 ZIP SHA-256: `d97bbde99ca3188ad3efeddf296cd19772df040688485ebcaf86a719a385336c`

## 성능 smoke

- Release app, menu-bar idle, 30초/5초 간격 6표본
- 평균 CPU: `0.000%`
- 평균 RSS: `93.9 MiB`
- 표본 RSS 범위: 약 `81.9~102.1 MiB`
- 이 결과는 smoke이며 계획의 10분·wakeups·장기 증가 추세 검증을 대체하지 않는다.

## 검증한 핵심 경계

- ratio `0`, `1`, 범위 밖, `NaN`, `±infinity`
- 부분 window, reset 누락, unknown limit type, malformed payload
- stale last-known-good source/observedAt 보존
- Claude Keychain JSON에서 access token만 선택하고 malformed/token 누락/credential 없음/접근 거부를 typed state로 구분
- Claude OAuth URL·GET·beta/User-Agent/no-body/no-cookie, 180초 success cache, 401·403·429·5xx·timeout mapping
- Claude 5시간·7일·Fable scoped window, ISO/epoch reset, partial·범위 밖·Boolean/malformed payload 처리
- Claude token·원본 payload는 probe/history/export/fixture에 저장하지 않고 credential 전후 불변 확인. 실계정 probe/repository에는 실제 quota 값을 남기지 않으며 앱 history에는 정규화된 ratio/reset만 로컬 저장
- Codex adapter method가 `initialize`, `account/rateLimits/read` 두 개뿐임
- Grok adapter URL이 정확한 first-party billing endpoint로 고정되고 GET·CLI auth header 외 body가 없음
- Grok 401·403·429·5xx·timeout typed state, 현재/legacy payload, reset 누락, invalid percent
- Grok probe의 redirect 거부와 실제 quota·token·user ID 비출력
- Process stdout/stderr 분리와 timeout
- credential symlink와 `0644` 거부, `0600` 허용
- history 손상 파일 quarantine와 90일 retention
- 같은 Provider refresh 6개 요청 single-flight 1회 수집
- 같은 reset window의 fresh 3표본에서만 pace 산출
- notification threshold/window dedupe
- redacted export에 diagnostic secret/PII 미포함

## 열려 있는 외부·수동 gate

- Claude OAuth observed 계약은 공식 stable API가 아니므로 Claude Code/endpoint schema 변경 시 재검증
- GLM 실제 CLI/wrapper 경로 확인 또는 공식 usage-query plugin 연결
- Z.ai app-owned Keychain add/read/replace/delete 실기기 통합 QA
- credential owner mismatch, HTTP cancellation, sleep/wake, network flap, CLI orphan soak
- 다중 display/Space/화면 가장자리 popover
- VoiceOver·키보드·Increase Contrast·Reduce Motion 수동 QA
- 10분 idle/wakeups 및 장기 memory trend
- Developer ID secure timestamp, notarization, stapling, `spctl`
- clean-account install 및 Sparkle upgrade/downgrade

외부 gate는 credential·feed·별도 환경이 없으므로 완료로 표시하지 않았다.

Provider 실환경 상세: [provider-validation-2026-08-16.md](provider-validation-2026-08-16.md)

## Grok LIVE 변경 회귀 확인

- Python safe validation 자체 테스트: `15 PASS`
- Python Grok billing probe 자체 테스트: `4 PASS`
- Fixture guard: `16 files PASS`
- Swift unit test: `29 PASS`
- Grok parser·adapter targeted Swift test: `10 PASS`
- Dashboard XCUITest: `1 PASS` (`Sign to Run Locally` ad-hoc 서명 사용)
- macOS Debug arm64 build: `PASS`, shipping Swift source warning 0
- 실계정 Grok billing probe: `HTTP 200`, 필요한 field 존재, credential SHA-256·mtime·mode 불변
- 실제 quota·잔액·token·user ID·원본 payload: repository와 보고서에 저장하지 않음

## Claude 자동 연결 회귀 확인

- Python safe validation 자체 테스트: `16 PASS`
- Python Claude OAuth probe 자체 테스트: `2 PASS`
- Fixture guard: `18 files PASS`
- Swift unit test: `36 PASS`; Claude OAuth/Keychain 관련 test method `5 PASS`
- Dashboard XCUITest: `1 PASS`; limit scroll 후 sidebar refresh button hittable 확인
- 실제 UI smoke: 연결 화면에 Claude path 입력 없음, Keychain 자동 탐색 toggle 활성, scroll 후 detail header clipping 정상
- 실계정 redacted probe: HTTP 200, 5시간·7일·Fable field 존재, Keychain credential 불변
- 최종 Universal Release 설치: Claude `available`, 3 windows, source `keychain`, diagnostic 없음
- 임시 QuotaBeacon statusLine bridge 제거 및 사용자 원래 statusLine 복원 후 앱 재시작: Claude LIVE 유지

## Beacon Ledger GUI 구현 검증

- Swift unit test: `40 PASS`; remaining/used 보완 관계, urgency 경계, reset 표기, Provider mark 고유성 포함
- XCUITest: `2 PASS`; 한도·설정·메뉴 요약·선택 상세 스크린샷 4종 보존
- Python provider/bridge test: `23 PASS` (`16 + 2 + 4 + 1`)
- Fixture guard: `18 files PASS`
- Originality, Greenfield reference, security audit: `PASS`
- Universal Release: `arm64 + x86_64`, hardened runtime, ad-hoc 서명 무결성 `PASS`
- 설치 경로: `/Users/hwanchoi/Applications/QuotaBeacon.app`, version `0.1.0 (1)`, 실행 process 확인
- 시각 QA: 430×560 popover의 Provider 전체 요약, 선택 row inline inspector, 760×572 한도·설정 화면의 고정 header 밖 clipping을 실제 XCUITest screenshot으로 확인
- 팝오버 선택 시 확장 content가 기존 scroll offset을 유지해 상단이 잘리는 현상을 `ScrollViewReader`의 선택 Provider top anchor로 수정
- 상세 상태에서 footer 설명이 압축되지 않도록 label layout priority와 dashboard button 고정 크기를 적용

### GUI QA 중 해결한 환경·테스트 이슈

- `GUI-QA-001`: 전면 Chrome/ego 창이 XCUITest 입력을 가로챔. 테스트 전 해당 창을 최소화한 뒤 전체 suite 재실행 `PASS`. 제품 결함이 아닌 일회성 test environment 이슈로 분류했다.
- `GUI-QA-002`: third-party `NSStatusItem`이 ControlCenter AX에서 안정적인 식별자로 노출되지 않음. status button AX metadata를 명시하고 UI-test 환경 전용 popover show hook을 추가했으며 production 경로는 유지했다. 재사용 교훈 후보는 반복되지 않아 이번에는 `not-a-lesson`으로 분류했다.
- `GUI-QA-003`: 선택 inspector 확장 직후 scroll content가 위로 밀려 상세 heading이 잘림. 선택 row를 scroll top에 고정하고 요약/상세 screenshot을 재확인했다. 동일 유형 반복 이력이 없어 `not-a-lesson`으로 분류했다.

## Reset Bands 추세 그래프 구현 검증

- Swift unit test: `48 PASS`; 신규 trend presentation test `8 PASS`
- 검증 범위: 24시간 cutoff/domain, Provider·window·reset 분리, freshness·큰 gap segment, 중복 LIVE 우선, bucket first/minimum/last 보존, 긴급 Provider 선택, 현재 fresh window rate/pace, reset countdown sub-second jitter 정규화
- XCUITest: `2 PASS`; 실제 History 기반 24시간 상단·plot, 7일, 30일, 전체 Provider screenshot과 기존 한도·설정·메뉴 popover 회귀를 함께 검증
- 실제 History QA: Claude 3개 window를 독립 line으로 표시하고 25%·10% 임계치, 수집 coverage, 최신 point, 변화 속도, reset 전 소진 없음 상태를 확인
- Python provider/bridge test: `23 PASS` (`16 + 2 + 4 + 1`)
- Fixture guard `18 files`, originality, Greenfield reference, security audit, Dev Lesson validation: `PASS`
- Universal Release: `arm64 + x86_64`, hardened runtime, ad-hoc 서명 무결성 `PASS`

### 추세 QA 중 해결한 이슈

- `TREND-QA-001` (`plan-only`): 최소 dashboard 높이에서 중복 label row와 28pt padding 때문에 plot이 첫 화면 아래로 과도하게 밀렸다. coverage를 header에 합치고 control row와 padding을 압축한 뒤 상단·scroll plot screenshot을 재검증했다.
- `TREND-QA-002` (`plan-only`): Claude OAuth reset countdown의 sub-second 흔들림을 raw timestamp identity로 사용해 매 refresh가 1-point segment가 되고 rate가 `계산 대기`로 남았다. reset minute identity를 trend와 pace에 적용하고 jitter 회귀 test를 추가해 실제 선·`-1.5%/시간` 계산을 확인했다.
- `TREND-QA-003` (`plan-only`): 지역화된 마지막 X축 시각 label이 plot 끝에서 ellipsis로 잘렸다. tick을 plot 내부 균등 위치로 제한하고 trailing plot padding을 적용했다.
- `TREND-QA-004` (`plan-only`): macOS segmented Picker가 개별 `Button` 대신 container `RadioGroup`으로 노출되어 최초 기간 전환 test가 실패했다. identifier가 있는 control의 정규화 좌표로 선택하도록 고쳐 최종 전체 UI test를 재통과했다.
