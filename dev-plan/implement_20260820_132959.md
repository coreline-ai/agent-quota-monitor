# implement_20260820_132959.md

작성 일시: `2026-08-20 13:29:59 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
현재 디스크 사용량 메뉴바 패치를 안전하게 완성하고 기존 quota·Provider·dashboard 흐름의 회귀를 0건으로 검증한다.

## 개발 범위
- 현재 미커밋 디스크 사용량 패치의 계약과 기준선을 고정한다.
- `/` 내부 볼륨과 `/Volumes` 후보의 local/internal 메타데이터를 기준으로 외장 볼륨을 판정하고 removable/ejectable은 보조 검증으로 보존한다.
- 디스크 조회를 Infrastructure actor와 취소 가능한 비동기 UI task로 분리한다.
- Application/Popover 조립 경계에서 `DiskUsageProviding`을 주입하고 기존 menu provider 흐름은 유지한다.
- DiskUsage 순수 계산·볼륨 선택·실제 root 조회·메뉴바 접근성 회귀를 검증한다.
- README·architecture·product-scope와 계획 상태를 실제 구현에 맞게 갱신한다.

## 제외 범위
- 새 Provider, quota 계약, credential, network 수집 변경
- 대시보드·추세·설정 화면의 기능 확장
- 새 외부 dependency 및 무관한 리팩터링

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260819_082840.md
- README.md
- docs/architecture.md
- docs/product-scope.md
- docs/qa-report-2026-08-16.md
- dev-plan/implement_20260819_082840.md

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- quota provider, credential, network, history, notification pipeline에는 변경을 가하지 않는다.
- 디스크 정보는 관찰 전용이며 history/export/log에 저장하지 않는다.
- 외장 후보가 없거나 접근할 수 없으면 badge를 숨기고 quota 상태·기존 menu layout은 그대로 유지한다.
- 각 Phase 완료 전에는 전체 변경 파일과 `git diff --check`를 확인한다.

## Phase 상태 요약
- [x] Phase 1. 계약·기준선 고정 완료
- [x] Phase 2. 디스크 서비스 경계 보정 완료
- [x] Phase 3. 메뉴바 연결 및 UI 회귀 완료
- [x] Phase 4. 전체 QA·문서·완료 게이트 완료

## QA 관점
- [x] `/` root, root symlink, hidden volume, network volume, non-local volume, simulator 경로를 포함한 비허용 후보, inaccessible volume을 metadata 정책으로 구분한다.
- [x] local·non-internal volume만 외장 후보로 허용하고 removable/ejectable 값은 보조 검증으로 기록하며 다중 후보 선택 순서를 결정론적으로 고정한다.
- [x] total/free가 0·음수·범위 초과일 때 invariant를 유지하고 누락 조회는 `nil`로 처리한다.
- [x] view가 disappear하거나 refresh가 재시작될 때 이전 disk task가 취소되고 늦은 결과가 새 상태를 덮지 않는지 확인한다.
- [x] 외장 volume I/O가 느리거나 실패해도 main UI가 멈추지 않는지 확인한다.
- [x] 기존 provider 표시 순서, filter, refresh, dashboard navigation, accessibility 흐름이 동일하게 통과한다.
- [x] 전체 build/test, fixture/security/originality audit을 최종 gate로 실행한다.

## 소스 오브 트루스와 데이터 흐름

- 원본: Foundation volume resource values와 FileManager가 반환하는 현재 volume capacity.
- 공유 계약: `AIQuotaMonitor/Domain/DiskUsageModels.swift`의 `DiskUsageProviding`과 target 계약. filesystem 경로와 구현은 Infrastructure에 숨긴다.
- 정규화: `DiskUsageInfo`가 total/free를 clamp하고 used/free ratio와 표시 문자열을 계산한다.
- 선택: 순수 volume metadata selector가 root symlink와 허용되지 않은 `/Volumes` 후보를 제거하고 결정론적으로 하나를 선택한다.
- 조립: `AppDelegate`/`StatusItemController`가 `DiskUsageProviding` 구현을 만들고 `PopoverController`를 통해 View에 주입한다.
- 표시: `MenuBarPlaceholderView`가 취소 가능한 task로 root/external 결과를 받아 badge만 갱신한다. history, export, quota snapshot에는 연결하지 않는다.

## 예상 변경 파일 / 영향 범위

- `AIQuotaMonitor/Infrastructure/System/DiskUsageService.swift` — actor, volume metadata 수집, 외장 후보 선택
- `AIQuotaMonitor/Domain/DiskUsageModels.swift` — DiskUsage invariant, target, providing contract
- `AIQuotaMonitor/Application/AppDelegate.swift` — 서비스 조립
- `AIQuotaMonitor/Application/StatusItemController.swift` — 서비스 전달
- `AIQuotaMonitor/Application/PopoverController.swift` — View 주입
- `AIQuotaMonitor/Features/MenuBar/MenuBarPlaceholderView.swift` — cancellable disk task와 badge 표시
- `AIQuotaMonitorTests/Domain/DiskUsageTests.swift` — 순수 계산·선택·경계·실제 root smoke
- `AIQuotaMonitorUITests/AIQuotaMonitorUITests.swift` — 메뉴바 disk badge 및 기존 흐름 회귀
- `README.md`, `docs/architecture.md`, `docs/product-scope.md` — 기능 및 경계 문서
- `dev-plan/implement_20260820_132959.md` — 실제 진행 상태 기록

---

## Phase 1. 계약·기준선 고정

### 목표
- 현재 작업 트리의 디스크 패치와 기존 provider-order 변경을 분리해 기준선으로 기록한다.
- 외장 volume의 의미와 단일 badge 정책을 문서로 고정한다.

### 구현 태스크
- [x] 현재 `git status`, 변경 파일, 기준 commit을 기록한다.
- [x] `/`는 내부 badge, `volumeIsLocal == true && volumeIsInternal == false`인 `/Volumes` volume은 외장 후보로 정의한다. removable/ejectable이 false인 고정형 외장 SSD도 누락하지 않는다.
- [x] 외장 후보가 여러 개면 resolved path 오름차순의 첫 후보만 표시하고, 후보가 없으면 외장 badge를 숨긴다.
- [x] disk 정보가 quota snapshot/history/export/notification에 들어가지 않는 경계를 확인한다.
- [x] 기존 provider-order 계획과 이번 patch의 소유 파일을 분리한다.

### 자체 테스트
- [x] 기존 Debug build와 전체 XCTest/XCUITest 기준선을 실행한다. (89 PASS: unit 85 + UI 4)
- [x] fixture guard, security audit, originality audit 기준선을 실행한다.
- [x] baseline 결과에서 이번 patch와 무관한 실패가 없는지 확인한다.

### 이슈 및 수정
- [x] `PLAN-001`: local/non-internal 기준, Domain contract 위치, CoreSimulator 문자열 필터 제거, View stub 검증 범위를 계획 검토에서 명시했다.

### 완료 조건
- [x] 계약·소유 경계 확정
- [x] 기준선 결과 기록
- [x] 다음 Phase 진행 가능


## Phase 2. 디스크 서비스 경계 보정

### 목표
- 외장 volume 오판을 제거하고 디스크 I/O를 main actor에서 분리한다.

### 구현 태스크
- [x] `DiskUsageProviding`과 `DiskUsageTarget`을 Domain에 두고 async 계약으로 정의한다.
- [x] `DiskUsageService`를 actor로 격리하고 Domain contract를 구현한다.
- [x] volume metadata에 local/internal/removable/ejectable/name/path/capacity를 보존한다.
- [x] root symlink, hidden, network/non-local volume을 필터링하고 `CoreSimulator` 문자열 추정 필터는 제거한다.
- [x] external candidate selector를 순수 결정론적 함수로 분리해 테스트 가능하게 한다.
- [x] resource values 실패 시 기존 FileManager capacity fallback을 유지하되, 유효한 total이 없으면 `nil`을 반환한다.
- [x] `DiskUsageInfo`의 total/free clamp를 normalized total 기준으로 명확히 하고 계산 invariant를 유지한다.

### 자체 테스트
- [x] total 0·음수, free 음수·total 초과, 0%·25%·100% 경계를 검증한다.
- [x] root symlink, internal, non-local, removable/ejectable metadata, hidden, 다중 후보 선택을 가짜 metadata로 검증한다.
- [x] 후보 없음과 실제 resource lookup 성공/실패 경계는 selector 및 root/external smoke 조합으로 검증한다. FileManager fallback은 구현 보존을 diff 검토로 확인한다.
- [x] 실제 `/` 조회 smoke test가 유효한 capacity를 반환하는지 확인한다.
- [x] actor 호출이 컴파일되고 source warning이 0건인지 확인한다. (DiskUsageTests 8 PASS, arm64 Debug build PASS)

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] service/selector 경계 구현 완료
- [x] 순수 및 infrastructure 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 메뉴바 연결 및 UI 회귀

### 목표
- 서비스 조립을 Application 경계로 이동하고, 메뉴바 badge를 취소 가능하게 갱신한다.

### 구현 태스크
- [x] AppDelegate/StatusItemController/PopoverController를 통해 `DiskUsageProviding`을 주입한다.
- [x] View에서 직접 `DiskUsageService`를 생성하지 않도록 수정한다.
- [x] `.task(id: model.lastRefreshAt)` 또는 동등한 취소 가능 task로 root/external 조회를 실행한다.
- [x] 늦게 완료된 이전 task가 새 상태를 덮지 않도록 cancellation guard를 둔다.
- [x] 기존 430×560 layout, density control, provider order/filter, accessibility identifier를 유지한다.
- [x] 외장 결과가 없을 때도 내부 badge와 기존 menu content가 정상 표시되게 한다.

### 자체 테스트
- [x] 메뉴바 UI test에서 내부 disk badge와 기존 provider 목록·순서·dashboard CTA를 검증한다. (`testMenuBarBeaconLedgerCanOpen` 1 PASS)
- [x] async `DiskUsageProviding` stub으로 root 성공·external 없음·service failure 계약을 검증하고, UI test에서는 실제 root badge와 external optional 상태를 확인한다. (`DiskUsageTests` contract PASS, UI `menu.diskUsage` PASS)
- [x] refresh 재실행과 View disappear에서 task cancellation 및 stale result 방지 코드를 확인하고 `.task(id:)` 경계를 적용한다.
- [x] 기준선에 있던 dashboard/sidebar/navigation UI predicate assertion을 유지한 채 전체 UI 4개를 통과시킨다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] Application 조립과 View task 연결 완료
- [x] 메뉴바/UI 회귀 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 4. 전체 QA·문서·완료 게이트

### 목표
- 회귀 0 기준을 전체 저장소 검증으로 확인하고 문서·계획 상태를 실제 결과와 맞춘다.

### 구현 태스크
- [x] README에 메뉴바의 local disk capacity 표시와 외장 volume 제한을 추가한다.
- [x] architecture에 disk service의 Application/Infrastructure/View 경계를 기록한다.
- [x] product-scope에 새 표시 범위와 외장 후보 없음 동작을 기록한다.
- [x] 최종 diff, diff check, 변경 파일, 미추적 파일을 검토한다.
- [x] rollback 시 history/defaults/provider 계약에 영향이 없음을 확인한다.

### 자체 테스트
- [x] `xcodebuild` Debug build와 전체 unit/UI test를 실행한다. (93 PASS: unit 89 + UI 4)
- [x] arm64 Debug build/test를 확인했다. Universal Release build는 이번 관찰 전용 메뉴바 patch의 packaging 범위 밖이므로 생략했다.
- [x] fixture guard, safe-validation tests, originality audit, security audit를 실행한다. (모두 PASS)
- [x] `git diff --check`와 source warning-as-error gate를 통과시킨다. (PASS)
- [x] 기존 quota/provider/parser/persistence/presentation 테스트가 모두 회귀 없이 통과하는지 확인한다. (전체 unit 89 PASS)

### 이슈 및 수정
- [x] 발견 이슈 없음. 선택식 없는 UI targeted command가 0개 테스트로 해석된 것은 클래스 포함 선택식으로 재실행해 1 PASS로 확인했다.

### 완료 조건
- [x] 구현·문서화 완료
- [x] 전체 회귀 0건
- [x] 최종 QA 결과 기록
- [x] 후속 리스크가 있으면 PASS로 숨기지 않고 별도 기록

## 롤백 기준

- 이번 patch는 history schema, UserDefaults, credential, provider contract를 변경하지 않는다.
- 문제가 발견되면 `PopoverController`의 주입과 View badge/task 연결을 되돌리고, 새 DiskUsage service/model/test 파일을 제거하는 방식으로 기능 단위 rollback이 가능해야 한다.
- 기존 provider-order 변경과 UI test 변경은 이번 patch rollback 대상에서 제외한다.

## 최종 결과 요약

- [x] 변경 파일과 범위가 계획과 일치한다.
- [x] 전체 테스트와 audit 결과가 기록되었다.
- [x] 회귀 0건을 확인했다.

### 최종 QA 기록

- `xcodebuild ... Debug ... test`: 93 passed, 0 failed, 0 skipped (unit 89 + UI 4)
- `DiskUsageTests`: 8 passed; root smoke 및 external optional 결과 포함
- `git diff --check`: PASS
- fixture guard: 21 files PASS
- safe-validation: 17 tests PASS
- Claude/Grok/GLM/Gemini provider probes: 각각 2/4/4/6 tests PASS
- originality audit 및 security audit: PASS
- 변경 범위: DiskUsage Domain/Infrastructure/Application/MenuBar/UI test와 README·architecture·product-scope·개발 계획으로 제한. quota provider, credential, network, history, export, notification 계약은 변경하지 않음.
